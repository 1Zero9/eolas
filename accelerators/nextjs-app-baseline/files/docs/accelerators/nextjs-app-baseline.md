# Next.js app baseline

This accelerator is the foundation for a new Next.js 14 project. Select it first, then layer compatible accelerators such as Prisma, PWA, or a component.

After Eolas creates the workspace, install the locked dependencies only after a separate explicit approval step.
